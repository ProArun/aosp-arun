android_app_import {
    name: "Hello",
    apk: "Hello.apk",
    
    // Retain the signature from Android Studio (debug key)
    //presigned: true,
    
    // Optimizations: Skip dex pre-opt if you want faster iteration during dev
    dex_preopt: {
        enabled: false,
    },
    certificate: "platform",
    // Partition: Install to /product/app/Hello
    product_specific: true,
}